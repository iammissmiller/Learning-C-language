#include <stdio.h>

#include <string.h>

int main()
{
    
    char str[] = "Geeks" ;
    
    printf("%s" , str);
    
    int length;
    
    length = strlen(str);
    
    printf("\n\nLength of the string is : %d" , length);
    
    return 0;
    
}
